export type SpaService = {
  id: string;
  serviceName: string;
  description: string;
  price: string;
};

export type PetProducts = {
  id: string;
  category: string;
  image: string;
  description: string;
  collection: string;
  price: string;
};
